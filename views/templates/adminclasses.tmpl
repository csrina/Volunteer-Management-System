{{define "adminusers"}}

{{template "adminheader" .}}

<main>
    <div>
        <button type="button">Edit ClassRoom</button>
        <button type="button">Add ClassRoom</button>
    </div>

{{template "footer" .}}

{{end}}