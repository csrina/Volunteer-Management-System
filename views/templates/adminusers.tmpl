{{define "adminusers"}}

{{template "adminheader"}}

<script id="tmpl_listUsers" type="x-dot-template">
    <table>
        <tr>
            <th>User ID</th>
            <th>Last Name</th>
            <th>First Name</th>
            <th>Username</th>
            <th>Email</th>
            <th>Phone</th>
            <th></th>
        </tr>
        {{`{{~it :value:index}}`}}
        <tr>
            <td>{{`{{=value.userId}}`}}</td>
            <td>{{`{{=value.lastName}}`}}</td>
            <td>{{`{{=value.firstName}}`}}</td>
            <td>{{`{{=value.userName}}`}}</td>
            <td>{{`{{=value.email}}`}}</td>
            <td>{{`{{=value.phoneNumber}}`}}</td>
            <td><button id="edit_{{`{{=value.userId}}`}}" type="button">Edit User</button>
                <button id="pass_{{`{{=value.userId}}`}}" type="button">Change Password</button></td>
        </tr>
    {{`{{~}}`}}
    </table>
</script>

<script id="tmpl_password" type="x-dot-template">
    <header>
        <h1>Edit password for user: </h1><h2>{{`{{=it.userName}}`}}</h2>
    </header>
    <table>
        <tr>
            <td> Password </td>
            <td><input id="pass1" type="password"></td>
        </tr>
        <tr>
            <td> Confirm Password </td>
            <td><input id="pass2" type="password"></td>
        </tr>
        <tr>
        <td><button id="Submit" type="button">Submit</button></td>
        <td><button id="cancel" type="button">Cancel</button></td>
        </tr>
    </div>
</script>

<script id="tmpl_listFamilies" type="x-dot-template">
    <table>
        <tr>
            <th>Family ID</th>
            <th>SurName</th>
            <th></th>
        </tr>
        {{`{{~it :value:index}}`}}
        <tr>
            <td>{{`{{=value.familyId}}`}}</td>
            <td>{{`{{=value.familyName}}`}}</td>
            <td><button id="edit_{{`{{=value.familyId}}`}}" type="button">Edit Family</button></td>
        </tr>
    {{`{{~}}`}}
    </table>
</script>

<script id="tmpl_newFamily" type="x-dot-template">
    <table>
        <tr> 
            <td>Family Name</td>
            <td><input id="famName" type="text"></td>
        </tr>
        <tr>
        <td>Parent One</td>
        <td>
            <select id="parent1">
                    <option value="">Select a Parent/Facilitator</option>
                {{`{{~it :value:index}}`}}
                    <option value="{{`{{=value.userId}}`}}">{{`{{=value.userName}}`}}</option>
                {{`{{~}}`}}    
            </select>
        </td>
        </tr>
        <tr>
        <td>Parent Two</td>
        <td>
            <select id="parent2">
                    <option value="">Select a Parent/Facilitator</option>
                {{`{{~it :value:index}}`}}
                    <option value="{{`{{=value.userId}}`}}">{{`{{=value.userName}}`}}</option>
                {{`{{~}}`}}    
            </select>
        </td>
        </tr>
            <td>Children</td>
            <td><input id="children" type="number"></td>
        </tr>
        <tr><td>
            <button id="submit" type="button">Submit</button>
            <button id="cancel" type="button">Cancel</button>
        </td></tr>
    </table>
</script>

<script id="tmpl_editFamily" type="x-dot-template">
    <div class="container">
        <div class="row">
            <div class="col-xs-3">
                <p> Family ID </p>
                <p> Family Name </p>
                <p> Parent One </p>
                <p> Parent Two </p>
                <p> Children </p>
            </div>
            <div class="col-xs-3">
                <p> {{=it.familyId}} </p>
                <input id="famName" type="text" value="{{`{{=it.familyName}}`}}">
                <select id="parent1">
                    <option value="{{`{{=it.parentOne}}`}}
                    {{`{{~it :value:index}}`}}
                    <option value="{{`{{=value.userId}}`}}">{{`{{=value.userName}}`}}</option>
                {{`{{~}}`}}
                    <option value="">Remove Parent</option>
                </select>
                <select id="parent1">
                {{`{{~it :value:index}}`}}
                    <option value="{{`{{=value.userId}}`}}">{{`{{=value.userName}}`}}</option>
                {{`{{~}}`}}
                    <option value="">Remove Parent</option>
                </select>


<script id="tmpl_editUser" type="x-dot-template">
    <table>
        <tr><td>
            ID
        </td><td>
            <p id="IDNum">{{`{{=it.userId}}`}}</p>
        </td></tr>
        <tr><td>
            Username
        </td><td>
            {{`{{=it.userName}}`}}
        </td></tr>
        <tr><td>
            First Name
        </td><td>
            <input id="fName" type="text" value="{{`{{=it.firstName}}`}}">
        </td></tr>
        <tr><td>
            Last Name 
        </td><td>
            <input id="lName" type="text" value="{{`{{=it.lastName}}`}}">
        </td></tr>
        <tr><td>
            Email
        </td><td>
            <input id="email" type="email" value="{{`{{=it.email}}`}}">
        </td></tr>
        <tr><td>
            Phone #
        </td><td>
            <input id="phoneNum" type="tel" value="{{`{{=it.phoneNumber}}`}}">
        </td></tr>
        <tr><td>
            <button id="submit" type="button">Submit</button>
            <button id="cancel" type="button">Cancel</button>
        </td></tr>
    </table>
</script>

<script id="tmpl_addUser" type="x-dot-template">
    <table>
        <tr>
            <td>User Role</td>
            <td><select id="role">
                    <option value="1">Facilitator</option>
                    <option value="2">Teacher</option>
            </select></td>
        </tr>
        <tr>
            <td>First Name</td>
            <td><input id="fName" type="text"></td>
        </tr>
        <tr>
            <td>Last Name</td>
            <td><input id="lName" type="text"></td>
        </tr>
        <tr>
            <td>Email</td>
            <td><input id="email" type="email"></td>
        </tr>
        <tr>
            <td>Phone #</td>
            <td><input id="phoneNum" type="tel"></td>
        </tr>
        <tr>
            <td>Password</td>
            <td><input id="pass1" type="password"></td>
        </tr>
        <tr>
            <td>Confirm Password</td>
            <td><input id="pass2" type="password"></td>
        </tr>
        <tr><td>
            <button id="submit" type="button">Submit</button>
            <button id="cancel" type="button">Cancel</button>
        </td></tr>
    </table>
</script>


<div id="usersSidenav" class="sidenav">
        <a onClick="familyList()">Edit Family</a>
        <a onClick="newFamily()">Add Family</a>
        <a onClick="userList()">Edit User</a>
        <a onClick="newUser()">Add User</a>
</div>

<div id="displayData" class="displayArea">Select an option to continue</div>

{{template "footer"}}

{{end}}